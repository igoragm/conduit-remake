import { ListPagination } from "./ListPagination";
import * as GetProps from "./ListPaginationProps";
import { SetState } from "../../../Utils/StateHandler";

export default SetState(ListPagination, GetProps);
